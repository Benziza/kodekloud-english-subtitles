{
  "name": "kodekloud-english-subtitles",
  "version": "2.0.0",
  "private": true,
  "description": "A Chrome and Edge extension that displays KodeKloud English subtitles in a separate companion tab.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate": "node -e \"JSON.parse(require('fs').readFileSync('manifest.json', 'utf8')); console.log('Manifest is valid JSON.')\""
  },
  "license": "MIT"
}
